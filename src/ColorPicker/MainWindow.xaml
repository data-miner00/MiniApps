<Window x:Class="ColorPicker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorPicker"
        mc:Ignorable="d"
        Title="Color Picker"
        Height="290" Width="300"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="90"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button x:Name="PickButton" Grid.Row="0" Content="Pick Color from Screen"
                Padding="8,6" Click="PickButton_Click" Margin="0,0,0,12"/>

        <Border x:Name="Swatch" Grid.Row="1" Background="#FFEFEFEF"
                BorderBrush="#FFB0B0B0" BorderThickness="1" CornerRadius="4"/>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Hex" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="HexBox" Grid.Column="1" IsReadOnly="True" VerticalContentAlignment="Center" Padding="4"/>
            <Button Grid.Column="2" Content="Copy" Margin="8,0,0,0" Padding="8,4" Click="CopyHex_Click"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="RGB" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="RgbBox" Grid.Column="1" IsReadOnly="True" VerticalContentAlignment="Center" Padding="4"/>
            <Button Grid.Column="2" Content="Copy" Margin="8,0,0,0" Padding="8,4" Click="CopyRgb_Click"/>
        </Grid>

        <TextBlock x:Name="StatusText" Grid.Row="4" Margin="0,10,0,0"
                   Foreground="Gray" FontSize="11" HorizontalAlignment="Center"/>
    </Grid>
</Window>
